# 简单外卖 App(HarmonyOS NEXT)项目大纲报告

> 毕设项目 | 技术栈:HarmonyOS NEXT / ArkTS / ArkUI | SDK 6.1.1(24) | 最后更新:2026-08-05

## 一、项目概述

**项目名称**:简单外卖(jiandanwaimai)
**项目定位**:对标美团外卖的学生点餐应用,HarmonyOS NEXT 毕业设计作品,展示鸿蒙现代技术栈(状态管理、持久化、路由、Canvas 可视化、动画)的完整业务闭环能力。
**核心目标**:
- 用户端:搜索 → 浏览店铺 → 加购 → 结算 → 下单 → 订单跟踪 → 评价 的完整点餐链路
- 商户端:接单 → 出餐 → 配送 → 完成的订单流转,以及店铺/商品管理与收入统计
- 数据可靠:App 重启后登录态、购物车、订单、收藏等关键数据不丢失

## 二、技术栈

| 领域 | 技术方案 | 说明 |
|---|---|---|
| 开发语言 | ArkTS(严格类型) | 禁止 any/unknown、无类型对象字面量 |
| UI 框架 | ArkUI 声明式 | 组件化 + @Builder 复用 |
| 状态管理 | AppStorage + @StorageLink/@StorageProp | 跨页面共享登录态/购物车/订单等 |
| 数据持久化 | @ohos.data.preferences | DataPersistence 服务统一管理 7 类关键数据 |
| 路由 | @ohos.router(pushUrl/back) | 13 个页面注册于 main_pages.json |
| 网络层 | @ohos/axios(预留) | HttpClient 封装,当前对接 Mock 数据,结构支持切换真实 API |
| 列表性能 | LazyForEach + cachedCount + Refresh | 店铺/订单/收藏列表懒加载与下拉刷新 |
| 数据可视化 | Canvas 自绘 | 近7天收入折线图、订单状态分布柱状图 |
| 动画 | setInterval + .animation() | 骑手配送位置跟踪动画、加载指示器 |

## 三、系统架构

```
┌──────────────────────────── 页面层(pages/) ────────────────────────────┐
│  Index(4 Tab: 首页/发现/订单/我的)                                      │
│    ├─ 首页: 搜索栏/分类/店铺列表(店铺详情=发现Tab)                        │
│    ├─ 发现: DiscoverPage + StoreDetailContent(收藏/评价/分类筛选/购物车)  │
│    ├─ 订单: OrderListMainPage(状态Tab筛选/下拉刷新)                      │
│    └─ 我的: ProfileMainPage(资产/菜单/商户入口)                          │
│  独立路由页: LoginPage / SearchPage / CheckoutPage / OrderDetailPage    │
│  / AddressListPage / CouponCenterPage / FavoriteListPage / ReviewPage   │
│  / ProfileEditPage / SettingsPage / MerchantCenterPage                  │
│  / MerchantOrdersPage / MerchantStatsPage                               │
├──────────────────────────── 组件层(components/) ────────────────────────┤
│  EmptyState / PriceText / RatingStars / ConfirmDialog                  │
│  / OrderStatusTimeline / LoadingIndicator                              │
├──────────────────────────── 服务层(service/) ───────────────────────────┤
│  AppStorageManager(业务状态操作) → MockDataService(数据唯一来源)          │
│  DataPersistence(load/save/clear+归一化) → PreferencesHelper(搜索历史)   │
│  HttpClient(axios 封装,预留真实API)                                     │
├──────────────────────────── 模型层(model/) ─────────────────────────────┤
│  Models.ets(User/StoreItem/GoodsItem/CartItem/Order/Address/Coupon     │
│  /Favorite/Review/DeliveryStep/MerchantStatsData 等 12+ 模型)           │
└──────────────────────────── 公共层(common/) ────────────────────────────┘
   Constants.ets(COLORS/SPACING/FONT_SIZE/BORDER_RADIUS/订单状态映射/API配置)
```

**启动数据流**:`EntryAbility.onCreate` 设置 appContext → `loadAllData()` 异步恢复持久化数据(经 normalizeData 归一化)→ `Index.aboutToAppear` 调 `initAppStorage()` 兜底初始化默认值。

## 四、功能模块

### 4.1 用户端
| 模块 | 页面 | 核心功能 |
|---|---|---|
| 首页 | Index(首页Tab) | 分类筛选、Banner、快捷入口、附近好店(懒加载+下拉刷新) |
| 搜索 | SearchPage | 热门词/历史搜索(preferences 持久化)、店铺搜索、点击进店 |
| 店铺详情 | DiscoverPage | 收藏切换、评价列表、商品分类筛选、购物车加减、去结算 |
| 结算 | CheckoutPage | 地址选择、优惠券选择、备注、金额计算、模拟支付(LoadingIndicator) |
| 订单 | OrderListMainPage / OrderDetailPage | 状态筛选、取消/确认收货/再来一单/去评价、状态时间轴、骑手配送动画+预估时间 |
| 地址 | AddressListPage | 增删改查、设置默认 |
| 优惠券 | CouponCenterPage | 可用/不可用/已用分类、领券 |
| 收藏 | FavoriteListPage | 收藏列表、取消收藏、点击进店 |
| 评价 | ReviewPage | 评分+标签+文字,提交后订单标记已评价 |
| 个人 | ProfileEditPage / SettingsPage | 修改昵称/手机号;清除缓存/退出登录/关于 |

### 4.2 商户端
| 模块 | 页面 | 核心功能 |
|---|---|---|
| 店铺管理 | MerchantCenterPage | 店铺信息编辑、商品增删改/上下架/改价 |
| 订单管理 | MerchantOrdersPage | 按状态 Tab 展示,接单→出餐→配送→完成流转 |
| 收入统计 | MerchantStatsPage | 今日/本周/累计数据卡片 + 近7天收入折线图 + 今日订单状态分布柱状图 |

### 4.3 通用能力
- 空状态统一(EmptyState)、加载态统一(LoadingIndicator)、评分展示统一(RatingStars)
- 金额/圆角/颜色统一常量管理

## 五、核心实现要点

1. **状态管理与持久化**:AppStorage 做内存共享,@StorageLink 双向绑定;7 类关键数据通过 DataPersistence 持久化;`loadAllData` 恢复时做**数据归一化**(normalizeData),兼容旧版本缺字段数据,从根上杜绝启动时 `undefined.length` 渲染崩溃。
2. **路由联动**:首页 4 Tab + 13 个独立路由页;从收藏页/搜索页点击店铺通过 `currentStoreId` + `onPageShow` 联动"发现"Tab 展示店铺详情。
3. **Canvas 数据可视化**:收入折线图(网格+折线+数据点+日期标签)与状态柱状图(5 状态配色),基于 `ctx.width/height` 自适应,数据由 service 层统计。
4. **配送跟踪可视化**:配送中状态展示骑手沿轨道移动动画与动态预估送达时间(基于下单时间推算)。
5. **列表性能**:IDataSource 自定义数据源 + LazyForEach + cachedCount,首页/订单/收藏列表按需渲染。

## 六、开发过程与任务完成情况

| 阶段 | 任务 | 状态 |
|---|---|---|
| 一 基础轮子 | 模型/常量扩展、状态管理升级、网络层与本地存储、通用组件(6个) | ✅ 完成 |
| 二 用户端链路 | 搜索/地址/结算/订单详情/优惠券/收藏/评价/个人信息/设置 9 个页面 | ✅ 完成 |
| 三 商户端链路 | 订单管理/收入统计/店铺信息编辑/商品管理 | ✅ 完成 |
| 四 体验集成 | 路由注册与导航联动、店铺详情增强、LazyForEach+Refresh、结算打通 | ✅ 完成 |
| 五 关键修复 | 12 个路由页面 @Entry、OrderDetailPage 集成、CheckoutPage 跳转 | ✅ 完成 |
| 六 持久化 | DataPersistence 持久化 + EntryAbility 初始化 + 数据归一化兼容 | ✅ 完成 |
| 七 集成补全 | 收藏/搜索返回店铺详情联动、搜索页返回修复 | ✅ 完成 |
| 八 商户图表 | 近7天收入折线图、今日订单状态柱状图 | ✅ 完成 |
| 九 配送跟踪 | 骑手位置动画、配送预估时间 | ✅ 完成 |
| 十 UI 打磨 | LoadingIndicator、店铺卡片视觉优化、按钮反馈 | ✅ 完成 |

**Git 记录**:共 30+ 次提交(master 分支),全部推送至 Gitee(`simple-takeout.git`),遵循 `<type>(<scope>): <描述>` 规范。

## 七、质量保障与问题修复

- **构建验证**:每次功能完成均执行 `assembleHap`,最终 `BUILD SUCCESSFUL`,产物 `entry-default-unsigned.hap` 正常生成
- **典型问题修复**:
  - Stack 组件误用 justifyContent/alignItems → 统一改为 alignContent(编译错误)
  - HorizontalAlign.Top 枚举误用 → VerticalAlign.Top
  - 模拟器启动崩溃(`Cannot read property length of undefined`)→ 持久化数据归一化 + 渲染防御
  - SettingsPage 文件损坏 → git 恢复并修复

## 八、项目成果

- 13 个路由页面 + 首页 4 Tab,用户端/商户端完整业务闭环
- 6 个通用组件,统一设计规范
- App 重启数据不丢失,持久化兼容旧版本数据
- 图表可视化、配送动画、加载动效等演示亮点
- 开发经验沉淀为 Skill(`.qoder/skills/arkts-takeout-dev`),含编译错误速查表与代码模式

## 九、后续展望(可选增强)

- Navigation + NavPathStack 替代 router(API 24 推荐方向,消除 deprecated 警告)
- PersistenceV2/relationalStore(SQLite)升级,支持大数据量
- @kit.MapKit 地图选点与真实骑手定位
- 对接真实后端 API(HttpClient 已预留结构)
- 单元测试补充(LocalUnit/ohosTest)

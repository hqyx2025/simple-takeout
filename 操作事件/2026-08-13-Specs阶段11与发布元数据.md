# Specs 阶段 11 与发布元数据记录

## 阶段 11：质量加固

- SubTask 22.1：结算金额纯函数与测试。
- SubTask 22.2：订单状态流转与终态保护测试。
- SubTask 22.3：持久化脏数据归一化测试。
- SubTask 22.4：Mock 店铺、商品、订单、优惠券和账号完整性测试。
- SubTask 22.5：登录、角色判断和重复注册测试。

所有 Local Test 均已注册到 `entry/src/test/List.test.ets`，并通过鸿蒙工程构建验证。

## 发布元数据核对

- `versionCode` 当前为 `1000000`，`versionName` 为 `1.0.0`。
- `bundleName` 当前为 `com.example.jiandanwaimai`，需在 AGC 创建应用时保持一致。
- `vendor` 已从模板值 `example` 改为项目维护者标识 `pengzhiqiang87`。
- 网络和前台定位权限均已配置 reason/usedScene；未发现超范围权限。
- 仍待外部发布流程完成：AGC 应用创建、发布证书/Profile、Signing Config、带签名 APP 和真机安装验证。

## 隐私合规实现

- 新增 `PrivacyPolicyPage.ets`，展示数据收集、用途、权限、保护和删除说明。
- 设置页新增隐私政策入口，并在 `main_pages.json` 注册页面。
- 首页首次出现时展示隐私同意弹窗；同意后将 `privacyConsent` 持久化，未同意不会自动放行。
- `privacyConsent` 已纳入 `DataPersistence` 持久化键列表。
- 公网隐私政策网页仍需部署到正式域名后填入 AGC，当前不能用本地页面替代该上架要求。

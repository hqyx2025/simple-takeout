# 首页图标与转场动画记录

## 完成功能

- 修复首页位置栏与搜索框布局：两者上下分行，搜索框使用全宽和弹性文本布局，搜索图标不再被挤到最左侧或消失。
- 新增项目内 SVG 矢量图标资源：
  - `entry/src/main/resources/base/media/location_icon.svg`
  - `entry/src/main/resources/base/media/search_icon.svg`
  - `entry/src/main/resources/base/media/chevron_down_icon.svg`
- 将首页定位、搜索、下拉箭头替换为上述矢量图标，移除本次新增的不可用 `sys.symbol.*` 资源引用。
- 顶部位置与搜索区域增加淡入、轻微上移动画，使用 ArkUI `opacity`、`translate` 和 `animation` 实现。

## 验证结果

- DevEco Hvigor 构建结果：`BUILD SUCCESSFUL`。
- `CompileArkTS`：通过，无 ERROR。
- 现有弃用 API 和资源冲突仅为原项目既有 WARN，本次未新增编译错误。

## 版本记录

- 本次变更应以 `fix(ui): replace home icons and animate header` 提交并推送到 Gitee。

## 后续 Specs 迭代：SubTask 22.1

- 新增 `entry/src/main/ets/service/OrderCalculations.ets`，统一商品总价和应付金额计算。
- 结算页改为调用纯函数，优惠券抵扣不会产生负数应付金额。
- 新增 `entry/src/test/OrderCalculations.test.ets`，覆盖正常金额、配送费、优惠券、负数边界。
- `entry/src/test/List.test.ets` 已注册新测试套件。
- DevEco Hvigor 构建结果：`BUILD SUCCESSFUL`，CompileArkTS 无 ERROR。

## 后续 Specs 迭代：SubTask 22.2

- 新增订单状态转移纯逻辑，覆盖正常生命周期、取消、退款和终态保护。
- 新增 `entry/src/test/OrderStatus.test.ets` 并注册到本地测试套件。

## 后续 Specs 迭代：SubTask 22.3

- 暴露 `DataPersistence.normalizeData` 纯归一化入口用于 Local Test。
- 新增脏用户、脏订单、损坏数组回退测试，验证持久化异常数据不会触发启动崩溃。

## 后续 Specs 迭代：SubTask 22.4

- 新增 Mock 数据完整性测试，覆盖分类、30 家店铺、每店 30 个商品、30 个订单、30 张优惠券和 5 个登录账号。
- 校验关键字段非空、金额和评分范围、集合数量及 ID/手机号唯一性。

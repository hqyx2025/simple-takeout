/node_modules
/oh_modules
/local.properties
/.idea
**/build
/.hvigor
.m2-repository/
.qoder/
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer
# 评价图片本地上传目录（运行时产物）
/server/uploads/
# 本地构建/验证日志
/build_check.log
/signing/**/*.p12
/signing/**/*.cer
/signing/**/*.p7b
/signing/**/*.csr
/signing/**/secrets*
# 本地签名接入文件（含密码，由 scripts/enable-release-signing.ps1 生成）
/signing/release-signing.json5
# 上架体检报告为生成物，不入库
/md/上架体检报告.md
# 临时产物目录：AI/agent 工作过程中产生的临时文件统一放这里（截图、界面元素树、构建日志、临时脚本等）。
# 该目录整体不入库（git 不会进入被忽略的目录，故留 .gitkeep 也无效）；
# 目录缺失时由脚本按需重建（如 scripts/device-test.ps1）。详见 AGENTS.md「项目约束」。
/TemporaryCacheStorage/

# 设备实测本地产物（截图 / 界面元素树）——历史路径，新产物一律写 TemporaryCacheStorage/
/device-screenshot.jpeg
/.ui-layout.json
/.ui-layout-probe.json
/.emu_start.log
/*.jpeg
/layout.json
/current-layout.json
/my-layout.json
/current-screen.jpeg

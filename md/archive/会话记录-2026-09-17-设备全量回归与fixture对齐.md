# 会话记录-2026-09-17-设备全量回归与fixture对齐

## 背景

用户要求「真机跑一遍」，实为模拟器（`127.0.0.1:5555`）全量设备回归。全程跑通后端四大回归脚本 + 全部页面级 UiTest 用例，并修掉一个真功能回归。

## 关键发现（环境真相）

| 端口 | 是什么 | 连的库 | 说明 |
|---|---|---|---|
| 9000 | Docker 容器 `takeout-app` | 容器 MySQL **3307**（40 店均匀数据） | 原来是 20 小时前旧镜像，**不含 goal 新功能** |
| 9001 | 本机 `mvn spring-boot:run` | 本机 MySQL **3306**（41 店累积数据） | 最新代码 |

- 设备 app 固定打 `10.0.2.2:9000`（容器 + 3307）。
- 回归脚本 fixture（StoreId=1/GoodsId=7/AddressId=27、checkout GoodsId=282 价 6.82）是 **3306 定标**，在 3307 里 `AddressId=27` 不存在、商品价全漂。

## 修复清单（均已推送）

| 提交 | 内容 |
|---|---|
| `49caff3` | **真功能回归**：`pay` 端点 channel 改 body→query param，修「无 body 调用 500」（RiderChain 10 FAIL → 13/13） |
| `b8b4a75` | 设备回归 fixture 对齐 3307 + 用例补登录/隐私同意前置 + review 骑手链 fixture |
| `edb9316` | SearchFlow 补登录前置兜底 |
| `5504aca` | SearchFlow 断言改 `search-store-1`（SearchPage 无 `search-result-list` 容器 id） |

另：`docker compose up -d --build app` 重建镜像，9000 → 最新代码（验证 `/api/stores/1/activities`、`/api/admin/dashboard` 返回 200）。

## fixture 对齐细节

- checkout：GoodsId 282→**40**（价 6.53，store2 星巴克 minOrder 17 fee 3），期望价 6.82→6.53、9.82→9.53、23.46→22.59、10.18→10.47。
- review/merchant/role：AddressId 27→**1**（3307 用户地址只有 1/2）；review fixture 从老商户链 `accept→deliver→complete` 改骑手链 `accept→ready→grab→pickup→deliver→confirm`。
- review 的 upload 目录检查去重：docker 下 uploads 是命名卷 `uploads-data`，host 本机 `server/uploads` 不互通，改为由 `review-image-url-ok`（GET 图 url 200）判定。

## 用例登录前置修复

`bm clean` 后 app 首启进**隐私同意页**，原 checkout/review 用例用 `loginRemote`（只写登录态不跳页）会卡登录页。统一改为：

1. `waitAny(privacy-accept)` → 点「同意并继续」；
2. 若仍在登录页 → 点「一键登录用户」（`quick-login-user`，走 `LoginPage.quickLogin` → `loginRemote` 落盘 + 跳首页）；已登录则跳过。

## 最终结果（全绿）

| 回归 | 结果 |
|---|---|
| rider-chain（纯 API 骑手链） | 13/13 |
| checkout（结算负路径 + 待付款 + 防重） | 全 PASS |
| review（图文评价上传/提交/渲染） | 全 PASS |
| merchant（接单→出餐 + 收入） | 全 PASS |
| role（骑手抢单/取餐/送达/收入 + 管理端） | 全 PASS |
| LoginFlow / LocationFlow / SearchFlow | PASS |
| StoreDetailCart / StoreCategoryFilter / BalancePage | PASS |

## 影响范围

| 侧 | 文件 |
|---|---|
| 后端 | `controller/OrderController.java`（pay channel 改 query param） |
| 脚本 | `device-checkout|review|merchant|role-regression.ps1` |
| 用例 | `CheckoutGuard/CheckoutFlow/ReviewFlow/SearchFlow.test.ets` |
| 环境 | Docker 镜像 rebuild（takeout-server:latest） |

## 验收

- 全部设备回归脚本 `RESULT: all checks passed` / `RESULT: passed`。
- 后端 `mvn test` 174 绿（未受影响）。
- 提交序：`49caff3` → `b8b4a75` → `edb9316` → `5504aca`，全部推送 master，工作区干净，无 AI 署名。


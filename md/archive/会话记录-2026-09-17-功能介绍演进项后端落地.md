# 会话记录-2026-09-17-功能介绍演进项后端落地

## 背景

按「简单外卖功能介绍」列出的待补/演进项逐项实现并提交推送（先做后端，前端另起一摊）。会话受同一 goal 驱动，逐项 commit + push。本会话完成 7 项，剩 6 项未做。

> 用户裁决口径：第三方 = Mock 适配层（支付走余额+模拟回调 / 短信固定码+日志 / OSS 接口化默认落本地）；明细表 = 最小侵入（只增 user_coupon + payment_record，order_item 保持 JSON 快照，order_status_log 不建）；多门店 = 全量做（暂缓未落地）。

## 已完成（7 项）

| # | 项 | 提交 | 说明 |
|---|----|------|------|
| 1 | 退款原因结构化 | `79bec3f` | `RefundReason` 枚举(5 code) + 备注；非法 code 拒绝，老客户端文本归 OTHER；refund_records 加 `reason_type` |
| 2 | 逻辑删除 | `6627030` | goods/categories/banners/announcements 加 `deleted`，删除改打标 + 查询过滤 |
| 3 | 地址经纬度+配送半径 | `149a647` | addresses 加 lat/lng，stores 加 `delivery_radius`，StoreView 出 `deliveryRadius`+`deliverable`，下单超范围拦截 |
| 4+5 | user_coupon/payment_record + 领取中心限领 | `387779e` | 两明细表落地，payment_record 写支付/退款；claimCoupon 终身限领一次 + 唯一键 |
| 6 | Mock 第三方适配层 | `a412bc8` | ObjectStorage 接口+本地实现；SmsSender+Mock 固定码；PaymentChannels(BALANCE/ALIPAY/WECHAT 走余额+模拟回调) |
| 7 | 营销活动(折扣/新客/满赠) | `68f9676` | marketing_activities 表，下单计价集成 DISCOUNT/NEW_USER/GIFT，`GET /stores/{id}/activities`，seeder 幂等演示数据 |

## 数据库变更

- 新表：`payment_records`、`user_coupons`（唯一键 uk_user_coupon_campaign）、`marketing_activities`
- 新增列：`refund_records.reason_type`、`goods/categories/banners/announcements.deleted`、`addresses.latitude/longitude`、`stores.delivery_radius`
- 全部由 schema.sql（新库）+ SchemaMigration（老库补列）幂等落地

## 测试

- `mvn -f server/pom.xml test` → **169 全绿**（会话开始 156，净增 13：SoftDeleteSqlTest 3、StoreDeliveryRadiusTest 3、UserCenterCouponClaimTest 1、PaymentChannelsTest 3、退款原因 2、营销 1）。

## 剩余未做（6 项，下轮继续）

- 套餐与组合购（setmeal 表 + 计价 + 前端）
- 管理端数据大屏（后端聚合 + 前端 Canvas 图表）
- 预约送达「送达方式」（expect_time 已存在，缺 delivery_type 配送/自取字段 + 前端）
- 菜品真实图片（后端 upload + goods.image 已就绪，剩前端渲染/上传入口）
- 多门店（商户 1:N + 切店 + 员工按店按角色鉴权，全量做）
- 前端 LazyForEach 改写

## 影响范围（后端为主）

- 新增类：`model/RefundReason`、`model/PaymentRecord`、`model/UserCoupon`、`model/MarketingActivity`、`dao/PaymentRecordDao`、`dao/UserCouponDao`、`service/thirdparty/*`（ObjectStorage/SmsSender/MockSmsSender/PaymentChannels）
- 改动：OrderService（退款/支付/折扣计价）、AdminService（退款+payment_record）、UserCenterService（领取中心限领）、StoreService（营销活动/配送半径）、StoreDao/OrderDao/AddressDao/GoodsDao（软删/坐标/活动/新客）、多个 controller（UserCenter/Store/Order/Auth/Upload）、DataSeeder、schema.sql、SchemaMigration
- 测试：OrderBoundaryTest、AssistantServiceTest、InputBoundaryTest、UserCenterAddressBoundaryTest、AdminServiceOrderFlowTest、AdminServiceProductPagingTest、OrderServiceFlowTest、OrderServiceSecurityTest、OrderServiceRiderFlowTest、UserCenterSearchTest、AuthControllerRateLimitTest 等按新构造签名同步

## 验收

- 7 项均先 `mvn test` 通过后 commit + push master，无 AI 署名。
- 提交序：`79bec3f` → `6627030` → `149a647` → `387779e` → `a412bc8` → `68f9676`。

